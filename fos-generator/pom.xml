<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<packaging>jar</packaging>

	<artifactId>fos-generator</artifactId>
	<parent>
		<groupId>gr.codelearn</groupId>
		<artifactId>fos-parent</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>

	<name>[${project.artifactId}]</name>
	<description>Synthetic order-event generator — continuous simulation of the food-order lifecycle for local dev and
		demos
	</description>

	<dependencies>
		<dependency>
			<groupId>gr.codelearn</groupId>
			<artifactId>fos-domain</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-json</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludeGroupIds>
						org.apache.xmlbeans,
						org.mapstruct,
						org.projectlombok
					</excludeGroupIds>
					<layers>
						<enabled>true</enabled>
					</layers>
					<mainClass>gr.codelearn.spring.kafka.generator.FosGeneratorApplication</mainClass>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
