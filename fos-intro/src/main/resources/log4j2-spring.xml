<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
	<Properties>
		<Property name="LOG_PATTERN">
			%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${hostName} - [%25.25t] %-30.30c{1.} : %m%n%ex
		</Property>
	</Properties>

	<Appenders>
		<Console name="ConsoleAppender" target="SYSTEM_OUT" follow="true">
			<PatternLayout pattern="${LOG_PATTERN}"/>
		</Console>

		<!-- Wrap it in AsyncAppender (no Disruptor needed) -->
		<Async name="AsyncConsole" bufferSize="262144" includeLocation="false">
			<AppenderRef ref="ConsoleAppender"/>
		</Async>
	</Appenders>

	<Loggers>
		<Logger name="gr.codelearn" level="debug" additivity="false">
			<AppenderRef ref="AsyncConsole"/>
		</Logger>
		<Logger name="org.springframework.kafka" level="info" additivity="false">
			<AppenderRef ref="AsyncConsole"/>
		</Logger>

		<Root level="info">
			<AppenderRef ref="AsyncConsole"/>
		</Root>
	</Loggers>
</Configuration>
